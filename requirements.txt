# MarketMind AI Python Package Requirements

# Core dependencies
requests==2.31.0
beautifulsoup4==4.12.3
pyyaml==6.0.1

# AI/LLM  (select provider via LLM_PROVIDER env var)
google-genai>=1.0.0          # gemini  – free tier available
openai>=1.0.0                # openai  – paid
anthropic>=0.25.0            # claude  – best quality, paid

# Data processing
pandas==2.2.0
numpy==1.26.3
yfinance==0.2.37

# Database
sqlalchemy==2.0.25

# Password hashing (REQUIRED for user registration/login)
bcrypt>=4.0.0

# Visualization / Report generation
matplotlib==3.8.2
seaborn==0.13.1
Pillow==10.2.0

# News fetching
feedparser==6.0.11

# Web Frontend
flask==3.0.2
flask-cors==4.0.0
flask-login>=0.6.3

# Scheduling
apscheduler==3.10.4

# Environment variables
python-dotenv==1.0.1
holidays>=0.50

# Market data – NSE bypass (Akamai cookie harvesting)
# playwright>=1.40.0 is installed separately; run: playwright install chromium
playwright>=1.40.0
curl-cffi>=0.6.0             # Chrome TLS fingerprint spoofing for NSE requests
# Finnhub uses the built-in `requests` library – no extra package needed.
# Alpha Vantage also uses `requests` – no extra package needed.

# Testing
pytest==7.4.4
pytest-cov==4.1.0
